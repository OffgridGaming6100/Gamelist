<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offgrid Gaming | Instant. Offline. Play.</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Teko:wght@600;700&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --bg-dark: #0b0d10;
            --bg-card: #151921;
            --bg-card-hover: #1c222d;
            --accent-orange: #ff5e00;
            --accent-glow: #ff7700;
            --border-color: #262e3d;
            --text-main: #f0f2f5;
            --text-muted: #8d98a7;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            background-image: 
                radial-gradient(circle at 50% 0%, rgba(255, 94, 0, 0.12) 0%, transparent 50%),
                linear-gradient(to right, rgba(255, 255, 255, 0.02) 1px, transparent 1px),
                linear-gradient(to bottom, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
            background-size: 100% 100%, 40px 40px, 40px 40px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Header section */
        header {
            text-align: center;
            padding: 45px 20px 25px;
            border-bottom: 1px solid var(--border-color);
            background: linear-gradient(180deg, rgba(11,13,16,0.95) 0%, rgba(11,13,16,0.8) 100%);
        }

        .logo-container {
            display: inline-block;
            margin-bottom: 10px;
        }

        .site-title {
            font-family: 'Teko', sans-serif;
            font-size: 3.5rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            color: #fff;
            text-shadow: 0 0 20px rgba(255, 94, 0, 0.5);
            line-height: 0.9;
        }

        .site-title span {
            color: var(--accent-orange);
        }

        .tagline {
            font-size: 0.95rem;
            font-weight: 700;
            letter-spacing: 5px;
            color: var(--accent-orange);
            text-transform: uppercase;
            margin-top: 5px;
        }

        /* Search Bar Only */
        .controls {
            max-width: 600px;
            margin: 30px auto 20px;
            padding: 0 20px;
            width: 100%;
        }

        .search-box {
            position: relative;
            width: 100%;
        }

        .search-box input {
            width: 100%;
            padding: 14px 16px 14px 45px;
            background: var(--bg-card);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: #fff;
            font-size: 1rem;
            outline: none;
            transition: all 0.3s ease;
        }

        .search-box input:focus {
            border-color: var(--accent-orange);
            box-shadow: 0 0 15px rgba(255, 94, 0, 0.3);
        }

        .search-box i {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-muted);
            font-size: 1.1rem;
        }

        /* Game Grid Container */
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 20px;
            flex: 1;
            width: 100%;
        }

        .game-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: 20px;
        }

        /* Game Cards */
        .game-card {
            background: var(--bg-card);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
            position: relative;
        }

        .game-card:hover {
            transform: translateY(-5px);
            border-color: var(--accent-orange);
            box-shadow: 0 5px 20px rgba(255, 94, 0, 0.25);
        }

        .game-card.selected {
            border: 2px solid var(--accent-orange);
            box-shadow: 0 0 15px rgba(255, 94, 0, 0.4);
        }

        .card-img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            background-color: #0d0f12;
            border-bottom: 1px solid var(--border-color);
        }

        .card-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            background: rgba(0, 0, 0, 0.85);
            color: var(--accent-orange);
            border: 1px solid var(--accent-orange);
            padding: 3px 8px;
            border-radius: 4px;
            font-size: 0.75rem;
            font-weight: bold;
        }

        .card-content {
            padding: 15px;
            display: flex;
            flex-direction: column;
            flex: 1;
        }

        .game-title {
            font-size: 1.05rem;
            font-weight: 700;
            color: #fff;
            margin-bottom: 5px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .game-meta {
            font-size: 0.8rem;
            color: var(--text-muted);
            margin-bottom: 12px;
            display: flex;
            justify-content: space-between;
        }

        .card-actions {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-top: auto;
        }

        .btn-play {
            width: 100%;
            padding: 10px;
            background: linear-gradient(135deg, #ff5e00 0%, #ff8800 100%);
            color: #fff;
            text-align: center;
            font-weight: bold;
            font-size: 0.85rem;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: none;
            cursor: pointer;
            transition: all 0.2s;
        }

        .game-card.selected .btn-play {
            background: #262e3d;
            color: var(--accent-orange);
            border: 1px solid var(--accent-orange);
        }

        .btn-specs {
            width: 100%;
            padding: 8px;
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-muted);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .btn-specs:hover {
            color: #fff;
            border-color: var(--accent-orange);
            background: rgba(255, 94, 0, 0.1);
        }

        /* Floating Bottom-Right Drawer */
        .cart-widget {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 320px;
            max-height: 420px;
            background: var(--bg-card);
            border: 1px solid var(--accent-orange);
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8);
            display: flex;
            flex-direction: column;
            z-index: 1000;
            overflow: hidden;
        }

        .cart-header {
            background: rgba(255, 94, 0, 0.15);
            padding: 12px 15px;
            font-weight: bold;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            font-size: 0.95rem;
        }

        .cart-items {
            list-style: none;
            padding: 10px;
            overflow-y: auto;
            max-height: 250px;
            flex: 1;
        }

        .cart-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 10px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 6px;
            margin-bottom: 6px;
            font-size: 0.85rem;
        }

        .cart-item-title {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 170px;
        }

        .cart-item-size {
            color: var(--accent-orange);
            font-weight: bold;
            margin-left: 8px;
        }

        .cart-item-remove {
            color: var(--text-muted);
            cursor: pointer;
            margin-left: 10px;
            transition: color 0.2s;
        }

        .cart-item-remove:hover {
            color: #ff4d4d;
        }

        .cart-footer {
            padding: 12px 15px;
            background: var(--bg-dark);
            border-top: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: bold;
        }

        .cart-total-size {
            font-size: 1.1rem;
            color: var(--accent-orange);
        }

        .empty-msg {
            color: var(--text-muted);
            font-size: 0.85rem;
            text-align: center;
            padding: 15px 0;
        }

        /* Modal Overlay for PC Requirements */
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(4px);
            z-index: 2000;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .modal-overlay.active {
            display: flex;
        }

        .modal-card {
            background: var(--bg-card);
            border: 1px solid var(--accent-orange);
            border-radius: 10px;
            width: 100%;
            max-width: 480px;
            padding: 24px;
            box-shadow: 0 10px 30px rgba(255, 94, 0, 0.2);
            position: relative;
            animation: fadeIn 0.2s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 12px;
            margin-bottom: 15px;
        }

        .modal-header h3 {
            color: #fff;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .modal-header h3 i {
            color: var(--accent-orange);
        }

        .modal-close {
            color: var(--text-muted);
            font-size: 1.2rem;
            cursor: pointer;
            transition: color 0.2s;
        }

        .modal-close:hover {
            color: #ff4d4d;
        }

        .specs-list {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 10px;
            font-size: 0.9rem;
        }

        .specs-list li {
            background: rgba(255, 255, 255, 0.03);
            padding: 10px;
            border-radius: 6px;
            border-left: 3px solid var(--accent-orange);
            display: flex;
            flex-direction: column;
            gap: 2px;
        }

        .specs-label {
            color: var(--text-muted);
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: bold;
        }

        .specs-value {
            color: #fff;
            font-weight: 500;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 25px;
            border-top: 1px solid var(--border-color);
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-top: 40px;
        }

        footer span {
            color: var(--accent-orange);
        }
    </style>
</head>
<body>

    <header>
        <div class="logo-container">
            <h1 class="site-title">OFFGRID <span>GAMING</span></h1>
        </div>
        <div class="tagline">Instant. Offline. Play.</div>
    </header>

    <!-- Search Bar Section -->
    <div class="controls">
        <div class="search-box">
            <i class="fa-solid fa-magnifying-glass"></i>
            <input type="text" id="searchInput" placeholder="Search games..." onkeyup="filterGames()">
        </div>
    </div>

    <!-- Games Grid -->
    <main class="container">
        <div class="game-grid" id="gameGrid">
            
            <!-- Game Card 1 -->
            <div class="game-card" data-id="1" data-title="Cyberpunk 2077" data-size="70">
                <span class="card-badge"><i class="fa-solid fa-wifi-slash"></i> OFFLINE</span>
                <img src="https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/1091500/header.jpg" alt="Cyberpunk 2077" class="card-img">
                <div class="card-content">
                    <div class="game-title">Cyberpunk 2077</div>
                    <div class="game-meta">
                        <span>Action / RPG</span>
                        <span>70 GB</span>
                    </div>
                    <div class="card-actions">
                        <button class="btn-play" onclick="toggleGameSelection('1')"><i class="fa-solid fa-plus"></i> Select Game</button>
                        <button class="btn-specs" onclick="openSpecs('1')"><i class="fa-solid fa-microchip"></i> Min PC Specs</button>
                    </div>
                </div>
            </div>

            <!-- Game Card 2 -->
            <div class="game-card" data-id="2" data-title="Grand Theft Auto V" data-size="100">
                <span class="card-badge"><i class="fa-solid fa-wifi-slash"></i> OFFLINE</span>
                <img src="https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/271590/header.jpg" alt="GTA V" class="card-img">
                <div class="card-content">
                    <div class="game-title">Grand Theft Auto V</div>
                    <div class="game-meta">
                        <span>Open World</span>
                        <span>100 GB</span>
                    </div>
                    <div class="card-actions">
                        <button class="btn-play" onclick="toggleGameSelection('2')"><i class="fa-solid fa-plus"></i> Select Game</button>
                        <button class="btn-specs" onclick="openSpecs('2')"><i class="fa-solid fa-microchip"></i> Min PC Specs</button>
                    </div>
                </div>
            </div>

            <!-- Game Card 3 -->
            <div class="game-card" data-id="3" data-title="Elden Ring" data-size="50">
                <span class="card-badge"><i class="fa-solid fa-wifi-slash"></i> OFFLINE</span>
                <img src="https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/1245620/header.jpg" alt="Elden Ring" class="card-img">
                <div class="card-content">
                    <div class="game-title">Elden Ring</div>
                    <div class="game-meta">
                        <span>RPG</span>
                        <span>50 GB</span>
                    </div>
                    <div class="card-actions">
                        <button class="btn-play" onclick="toggleGameSelection('3')"><i class="fa-solid fa-plus"></i> Select Game</button>
                        <button class="btn-specs" onclick="openSpecs('3')"><i class="fa-solid fa-microchip"></i> Min PC Specs</button>
                    </div>
                </div>
            </div>

            <!-- Game Card 4 -->
            <div class="game-card" data-id="4" data-title="Forza Horizon 5" data-size="110">
                <span class="card-badge"><i class="fa-solid fa-wifi-slash"></i> OFFLINE</span>
                <img src="https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/1551360/header.jpg" alt="Forza Horizon 5" class="card-img">
                <div class="card-content">
                    <div class="game-title">Forza Horizon 5</div>
                    <div class="game-meta">
                        <span>Racing</span>
                        <span>110 GB</span>
                    </div>
                    <div class="card-actions">
                        <button class="btn-play" onclick="toggleGameSelection('4')"><i class="fa-solid fa-plus"></i> Select Game</button>
                        <button class="btn-specs" onclick="openSpecs('4')"><i class="fa-solid fa-microchip"></i> Min PC Specs</button>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <!-- Lower Right Selected List Widget -->
    <div class="cart-widget">
        <div class="cart-header">
            <span><i class="fa-solid fa-list-check"></i> Selected Games</span>
            <span id="selectedCount" style="color: var(--accent-orange);">(0)</span>
        </div>
        <ul class="cart-items" id="cartItems">
            <div class="empty-msg">No games selected yet.</div>
        </ul>
        <div class="cart-footer">
            <span>Total Size:</span>
            <span class="cart-total-size" id="totalSize">0 GB</span>
        </div>
    </div>

    <!-- Modal Window for Minimum System Specs -->
    <div class="modal-overlay" id="specsModal" onclick="closeSpecsOnOuterClick(event)">
        <div class="modal-card">
            <div class="modal-header">
                <h3 id="modalGameTitle"><i class="fa-solid fa-desktop"></i> Game Title</h3>
                <i class="fa-solid fa-xmark modal-close" onclick="closeSpecs()"></i>
            </div>
            <ul class="specs-list" id="modalSpecsContent">
                <!-- Requirements inserted by JS -->
            </ul>
        </div>
    </div>

    <footer>
        <p>&copy; 2026 <span>OFFGRID GAMING</span>. All games optimized for instant offline play.</p>
    </footer>

    <script>
        // Track selected games
        let selectedGames = new Map();

        // Game Database with System Requirements
        const gameSpecsData = {
            "1": {
                title: "Cyberpunk 2077 - Minimum Specs",
                os: "64-bit Windows 10",
                cpu: "Intel Core i7-6700 or AMD Ryzen 5 1600",
                ram: "12 GB RAM",
                gpu: "NVIDIA GeForce GTX 1060 6GB / AMD Radeon RX 580 8GB",
                directx: "Version 12",
                storage: "70 GB SSD"
            },
            "2": {
                title: "Grand Theft Auto V - Minimum Specs",
                os: "Windows 10 / 11 64-Bit",
                cpu: "Intel Core 2 Quad Q6600 2.40GHz / AMD Phenom 9850",
                ram: "4 GB RAM",
                gpu: "NVIDIA 9800 GT 1GB / AMD HD 4870 1GB",
                directx: "Version 10 / 11",
                storage: "100 GB available space"
            },
            "3": {
                title: "Elden Ring - Minimum Specs",
                os: "Windows 10",
                cpu: "Intel Core i5-8400 / AMD Ryzen 3 3300X",
                ram: "12 GB RAM",
                gpu: "NVIDIA GeForce GTX 1060 3GB / AMD Radeon RX 580 4GB",
                directx: "Version 12",
                storage: "60 GB available space"
            },
            "4": {
                title: "Forza Horizon 5 - Minimum Specs",
                os: "Windows 10 (v18362.0 or higher)",
                cpu: "Intel Core i5-4460 / AMD Ryzen 3 1200",
                ram: "8 GB RAM",
                gpu: "NVIDIA GeForce GTX 970 / AMD Radeon RX 470",
                directx: "Version 12",
                storage: "110 GB available space"
            }
        };

        // Search Filter Logic
        function filterGames() {
            const query = document.getElementById('searchInput').value.toLowerCase();
            const cards = document.querySelectorAll('.game-card');

            cards.forEach(card => {
                const title = card.getAttribute('data-title').toLowerCase();
                card.style.display = title.includes(query) ? 'flex' : 'none';
            });
        }

        // Toggle Selection
        function toggleGameSelection(id) {
            const card = document.querySelector(`.game-card[data-id="${id}"]`);
            const title = card.getAttribute('data-title');
            const size = parseFloat(card.getAttribute('data-size'));

            if (selectedGames.has(id)) {
                selectedGames.delete(id);
                card.classList.remove('selected');
                card.querySelector('.btn-play').innerHTML = `<i class="fa-solid fa-plus"></i> Select Game`;
            } else {
                selectedGames.set(id, { title, size });
                card.classList.add('selected');
                card.querySelector('.btn-play').innerHTML = `<i class="fa-solid fa-check"></i> Selected`;
            }

            updateListUI();
        }

        // Remove item from selection
        function removeFromList(id) {
            if (selectedGames.has(id)) {
                toggleGameSelection(id);
            }
        }

        // Update Bottom-Right List & Total Size
        function updateListUI() {
            const listContainer = document.getElementById('cartItems');
            const totalSizeElement = document.getElementById('totalSize');
            const countElement = document.getElementById('selectedCount');

            listContainer.innerHTML = '';
            let totalGB = 0;

            if (selectedGames.size === 0) {
                listContainer.innerHTML = '<div class="empty-msg">No games selected yet.</div>';
            } else {
                selectedGames.forEach((game, id) => {
                    totalGB += game.size;

                    const li = document.createElement('li');
                    li.className = 'cart-item';
                    li.innerHTML = `
                        <span class="cart-item-title" title="${game.title}">${game.title}</span>
                        <div>
                            <span class="cart-item-size">${game.size} GB</span>
                            <i class="fa-solid fa-xmark cart-item-remove" onclick="event.stopPropagation(); removeFromList('${id}')" title="Remove"></i>
                        </div>
                    `;
                    listContainer.appendChild(li);
                });
            }

            totalSizeElement.innerText = `${totalGB} GB`;
            countElement.innerText = `(${selectedGames.size})`;
        }

        // Open PC Specs Modal
        function openSpecs(id) {
            const specs = gameSpecsData[id];
            if (!specs) return;

            document.getElementById('modalGameTitle').innerHTML = `<i class="fa-solid fa-desktop"></i> ${specs.title}`;
            
            const specsContent = `
                <li><span class="specs-label">OS</span><span class="specs-value">${specs.os}</span></li>
                <li><span class="specs-label">Processor</span><span class="specs-value">${specs.cpu}</span></li>
                <li><span class="specs-label">Memory</span><span class="specs-value">${specs.ram}</span></li>
                <li><span class="specs-label">Graphics</span><span class="specs-value">${specs.gpu}</span></li>
                <li><span class="specs-label">DirectX</span><span class="specs-value">${specs.directx}</span></li>
                <li><span class="specs-label">Storage</span><span class="specs-value">${specs.storage}</span></li>
            `;

            document.getElementById('modalSpecsContent').innerHTML = specsContent;
            document.getElementById('specsModal').classList.add('active');
        }

        // Close Specs Modal
        function closeSpecs() {
            document.getElementById('specsModal').classList.remove('active');
        }

        function closeSpecsOnOuterClick(event) {
            if (event.target.id === 'specsModal') {
                closeSpecs();
            }
        }
    </script>
</body>
</html>
